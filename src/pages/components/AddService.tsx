import { FaTools } from 'react-icons/fa';

type AddServiceButtonProps = {}

export const AddServiceButton = ({}: AddServiceButtonProps) => (
  <button className="add-service-button">
    <FaTools size={24} />
  </button>
);